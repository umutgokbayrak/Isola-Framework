<#compress><#if elementId[0] == '$'>	<#if elementId[1..] == 'document'>		var tmpObj = ${elementId[1..]}.documentElement;	<#else>		var tmpObj = ${elementId[1..]};	</#if><#else>	var tmpObj = document.getElementById('${elementId}');</#if>if (tmpObj.addEventListener) {	tmpObj.addEventListener('${eventName[2..]}',${jsFunct},false);} else if (tmpObj.attachEvent && !window.opera) {	tmpObj.attachEvent('${eventName}',${jsFunct});}</#compress>